<div class="card mt-3 mb-3 bg-dark text-white">
  <div class="card-body">
    <span class="mr-1"><%= comment.created_at.strftime("%Y-%m-%d") %></span>
    <span class="mr-1"><%= link_to comment.user.email, profile_path(comment.user) %></span> <br>
    <p><%= comment.body %></p>
  </div>
</div>
