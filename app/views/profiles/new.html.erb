<p>new</p>
<%= form_for([@user.build_profile], local: true ) do |f| %>
  <div class="field">
    <%= f.label :about %>
    <%= f.text_area :about %>
  </div>
  <div class="field">
    <%= f.label :image %>
    <%= f.file_field :image %>
  </div>
  <%= f.submit 'Submit' %>
<% end %>
